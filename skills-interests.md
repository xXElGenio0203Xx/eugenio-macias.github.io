---
layout: default
title: Skills 
---

<section class="fx-bg" aria-hidden="true"></section>

<div class="center">
  <h1 class="title-xl">Skills & More</h1>
  <p class="subtitle">A quick, high-contrast view of tools I use, libraries I love, and places I’ve explored.</p>
</div>

<section class="cards-grid">

  <!-- Office + Languages -->
  <article class="glass card tilt" aria-label="Productivity & Languages">
    <h2 class="card-title">Productivity</h2>
    <div class="chip-cloud">
      <span class="chip"><i class="fas fa-file-excel"></i> Excel</span>
      <span class="chip"><i class="fas fa-file-powerpoint"></i> PowerPoint</span>
      <span class="chip"><i class="fas fa-file-word"></i> Word</span>
      <span class="chip"><i class="fas fa-file-alt"></i> LaTeX</span>
    </div>

    <h2 class="card-title mt">Languages</h2>
    <div class="lang-list">
      <div class="lang">
        <div class="lang-head"><i class="fas fa-globe"></i> Spanish — Native</div>
        <div class="bar"><span style="width:100%"></span></div>
      </div>
      <div class="lang">
        <div class="lang-head"><i class="fas fa-globe"></i> English — Bilingual</div>
        <div class="bar"><span style="width:95%"></span></div>
      </div>
      <div class="lang">
        <div class="lang-head"><i class="fas fa-globe"></i> German — B1</div>
        <div class="bar"><span style="width:60%"></span></div>
      </div>
      <div class="lang">
        <div class="lang-head"><i class="fas fa-globe"></i> French — B1</div>
        <div class="bar"><span style="width:60%"></span></div>
      </div>
    </div>
  </article>

  <!-- Programming Tools -->
  <article class="glass card tilt" aria-label="Programming Tools">
    <h2 class="card-title">Programming Tools</h2>
    <div class="chip-cloud">
      <span class="chip"><i class="fab fa-python"></i> Python</span>
      <span class="chip"><i class="fab fa-java"></i> Java</span>
      <span class="chip"><i class="fas fa-code"></i> C</span>
      <span class="chip"><i class="fas fa-code"></i> C++</span>
      <span class="chip"><i class="fab fa-r-project"></i> R</span>
      <span class="chip"><i class="fab fa-docker"></i> Docker</span>
      <span class="chip"><i class="fab fa-github"></i> GitHub</span>
      <span class="chip"><i class="fas fa-code"></i> ReasonML</span>
      <span class="chip"><i class="fas fa-code"></i> DrRacket</span>
      <span class="chip"><i class="fas fa-chart-bar"></i> STATA</span>
    </div>
  </article>

  <!-- Libraries -->
  <article class="glass card tilt" aria-label="Libraries & Frameworks">
    <h2 class="card-title">Libraries</h2>
    <div class="chip-cloud">
      <span class="chip"><i class="fas fa-brain"></i> TensorFlow</span>
      <span class="chip"><i class="fas fa-flask"></i> PyTorch</span>
      <span class="chip"><i class="fas fa-square-root-alt"></i> NumPy</span>
      <span class="chip"><i class="fas fa-database"></i> Pandas</span>
      <span class="chip"><i class="fas fa-running"></i> Gymnasium</span>
      <span class="chip"><i class="fas fa-chess"></i> OpenSpiel</span>
      <span class="chip"><i class="fas fa-graduation-cap"></i> scikit-learn</span>
      <span class="chip"><i class="fas fa-chart-line"></i> Matplotlib</span>
    </div>
  </article>

  <!-- Interests -->
  <article class="glass card tilt interest" aria-label="Interests">
    <h2 class="card-title">Interests</h2>
    <ul class="interest-list">
      <li><i class="fas fa-chess"></i> Competitive Chess — <span class="pill">2000+ Elo</span></li>
      <li><i class="fas fa-guitar"></i> Classical Guitar — <span class="pill">National 2nd in Mexico</span></li>
      <li><i class="fas fa-superscript"></i> Probability & Stochastic Processes</li>
      <li><i class="fas fa-chart-line"></i> Quantitative Strategies for Investing</li>
      <li><i class="fas fa-utensils"></i> Trying New Food & Culinary Travel</li>
    </ul>
  </article>

  <!-- Countries visited -->
  <article class="glass card tilt flags" aria-label="Countries visited">
    <h2 class="card-title">Countries I’ve been to <span class="count" id="visited-count">(0)</span></h2>
    <div class="flag-grid visited" id="visited">
      <span class="flag-chip">🇲🇽</span><span class="flag-chip">🇺🇸</span><span class="flag-chip">🇨🇦</span><span class="flag-chip">🇨🇴</span>
      <span class="flag-chip">🇵🇹</span><span class="flag-chip">🇪🇸</span><span class="flag-chip">🇫🇷</span><span class="flag-chip">🇩🇪</span>
      <span class="flag-chip">🇳🇱</span><span class="flag-chip">🇨🇿</span><span class="flag-chip">🇦🇹</span><span class="flag-chip">🇸🇪</span>
      <span class="flag-chip">🇨🇭</span><span class="flag-chip">🇱🇺</span><span class="flag-chip">🇸🇮</span><span class="flag-chip">🇭🇷</span>
      <span class="flag-chip">🇧🇦</span><span class="flag-chip">🇲🇪</span><span class="flag-chip">🇷🇸</span><span class="flag-chip">🇬🇷</span>
      <span class="flag-chip">🇹🇷</span><span class="flag-chip">🇪🇬</span><span class="flag-chip">🇵🇷</span><span class="flag-chip">🇭🇺</span>
      <span class="flag-chip">🇲🇦</span><span class="flag-chip">🇵🇦</span><span class="flag-chip">🇳🇴</span><span class="flag-chip">🇬🇧</span>
      <span class="flag-chip">🇩🇰</span>
    </div>
    <p class="note">Currently at <strong>34 countries</strong> total traveled.</p>
  </article>

  <!-- Countries wishlist -->
  <article class="glass card tilt flags" aria-label="Countries wishlist">
    <h2 class="card-title">Next on my list <span class="count" id="wish-count">(0)</span></h2>
    <div class="flag-grid wishlist" id="wishlist">
      <span class="flag-chip">🇲🇳</span><span class="flag-chip">🇱🇧</span><span class="flag-chip">🇰🇷</span><span class="flag-chip">🇯🇵</span>
      <span class="flag-chip">🇹🇭</span><span class="flag-chip">🇻🇳</span><span class="flag-chip">🇮🇳</span><span class="flag-chip">🇸🇬</span>
      <span class="flag-chip">🇭🇰</span>
    </div>
  </article>

</section>

<style>
  :root{
    --bg:#0a0b0e; --ink:#ffffff; --muted:#e5e7eb; --line:#23262d; --card:#0f1115;
    --g1:#7c83ff; --g2:#22d3ee; --g3:#34d399; --g4:#f59e0b;
    --shadow:0 24px 60px rgba(0,0,0,.55);
  }
  body { color: var(--ink); background: var(--bg); }

  .title-xl{
    font-size: clamp(1.8rem, 3.2vw, 2.4rem);
    margin: 8px 0 2px;
    color: var(--ink);
  }
  .subtitle{ color: var(--muted); margin: 0 0 18px; }

  .fx-bg{
    position: fixed; inset:-10vmax; z-index:-1; pointer-events:none;
    background:
      radial-gradient(60% 50% at 20% 20%, rgba(124,131,255,.18), transparent 60%),
      radial-gradient(45% 40% at 80% 30%, rgba(34,211,238,.14), transparent 60%),
      radial-gradient(55% 45% at 50% 75%, rgba(52,211,153,.12), transparent 70%);
    filter: blur(55px) saturate(120%);
    animation: drift 22s ease-in-out infinite alternate;
  }
  @keyframes drift{ 0%{ transform: translate3d(0,0,0) scale(1); } 100%{ transform: translate3d(2vw,-1vh,0) scale(1.04); } }

  .cards-grid{
    max-width: 1100px; margin: 0 auto; padding: 0 16px 40px;
    display: grid; gap: 14px;
    grid-template-columns: 1fr;
  }
  @media (min-width: 980px){
    .cards-grid{ grid-template-columns: 1fr 1fr; }
    .cards-grid .flags{ grid-column: span 2; }
  }

  .glass.card{
    position: relative; overflow: hidden;
    background: linear-gradient(180deg, rgba(255,255,255,.08), rgba(255,255,255,.04));
    border: 1px solid var(--line); border-radius: 16px;
    padding: 14px 16px; box-shadow: var(--shadow);
    transition: transform .2s ease, box-shadow .2s ease, border-color .2s ease;
  }
  .glass.card:hover{
    transform: translateY(-3px) rotateX(1.2deg) rotateY(-1.2deg);
    border-color:#2a2f39; box-shadow: 0 24px 60px rgba(0,0,0,.35);
  }
  .glass.card::after{
    content:""; position:absolute; inset:-2px; border-radius:18px; opacity:0;
    background: conic-gradient(from 0deg, var(--g1), var(--g2), var(--g3), var(--g1));
    filter: blur(16px); transition: opacity .25s ease;
  }
  .glass.card:hover::after{ opacity:.25; }

  .glass.card::before{
    content:""; position:absolute; top:0; left:-40%; width:40%; height:100%;
    background: linear-gradient(90deg, transparent, rgba(255,255,255,.10), transparent);
    transform: skewX(-20deg); opacity:0;
  }
  .glass.card:hover::before{ animation: sheen .9s ease; opacity:1; }
  @keyframes sheen{ from{ left:-40%; } to{ left:120%; } }

  .card-title{
    margin: 2px 0 10px; font-size: 1.02rem; font-weight: 800; letter-spacing:.2px;
    display: inline-flex; align-items: center; gap: 8px;
  }
  .card-title::after{
    content:""; display:block; height:2px; width:64px; margin-left:10px; border-radius:2px;
    background: linear-gradient(90deg, var(--g1), var(--g2), var(--g3));
  }
  .mt{ margin-top: 14px; }

  .chip-cloud{ display:flex; flex-wrap:wrap; gap:8px; }
  .chip{
    --bd: 1px;
    position: relative;
    display:inline-flex; align-items:center; gap:8px;
    padding: 8px 12px; border-radius: 999px;
    background: rgba(255,255,255,.06);
    border: var(--bd) solid #2a2f39;
    color: #fff; font-weight: 700; letter-spacing:.2px;
    transition: transform .15s ease, box-shadow .15s ease, background .15s ease, border-color .15s ease;
  }
  .chip i{ opacity:.95; }
  .chip:hover{
    transform: translateY(-2px) scale(1.02);
    background: rgba(255,255,255,.10);
    border-color: transparent;
    box-shadow: 0 10px 24px rgba(0,0,0,.30);
  }
  .chip::after{
    content:""; position:absolute; inset:-1px; border-radius:999px; opacity:0;
    background: conic-gradient(from 0deg, var(--g1), var(--g2), var(--g3), var(--g1));
    filter: blur(10px); transition: opacity .2s ease;
  }
  .chip:hover::after{ opacity:.35; }

  /* Language bars */
  .lang-list{ display:grid; gap:10px; }
  .lang-head{ font-weight: 700; margin-bottom: 6px; }
  .bar{
    background:#11151c; border:1px solid #2a2f39; border-radius:999px; height:10px; overflow:hidden;
  }
  .bar span{
    display:block; height:100%;
    background: linear-gradient(90deg, var(--g1), var(--g2), var(--g3));
    border-radius:999px;
    animation: fill 1.1s ease forwards;
  }
  @keyframes fill{ from{ width:0 } }

  /* Interests */
  .interest .interest-list{ list-style:none; padding:0; margin:0; display:grid; gap:10px; }
  .interest .interest-list li{ display:flex; align-items:center; gap:10px; }
  .pill{
    font-size:.72rem; padding:3px 8px; border-radius:999px; color:#0b1220;
    background: linear-gradient(90deg, var(--g2), var(--g3));
    font-weight:800;
  }

  /* Flags */
  .flags .note{ margin-top:10px; color: var(--muted); }
  .flag-grid{
    display:flex; flex-wrap:wrap; gap:8px; align-items:center;
    padding-top: 2px;
  }
  .flag-chip{
    font-size: 20px; line-height: 1; padding: 8px 10px;
    border:1px solid #2a2f39; border-radius: 12px; background: rgba(255,255,255,.04);
    transition: transform .15s ease, box-shadow .15s ease, border-color .15s ease, background .15s ease;
    display:inline-flex; align-items:center; justify-content:center; min-width: 44px;
  }
  .flag-chip:hover{
    transform: translateY(-2px) scale(1.05);
    box-shadow: 0 10px 24px rgba(0,0,0,.30);
    border-color: transparent; background: rgba(255,255,255,.08);
  }
  .count{
    font-size:.8rem; font-weight:800; color:#0b1220; margin-left:8px;
    padding: 2px 8px; border-radius: 999px;
    background: linear-gradient(90deg, var(--g2), var(--g3));
  }

  .center { text-align:center; }

  /* Reduced motion accessibility */
  @media (prefers-reduced-motion: reduce){
    .fx-bg, .glass.card, .chip, .flag-chip, .bar span{ animation: none !important; transition: none !important; }
  }
</style>

<script>
  // 3D tilt (subtle) for cards on mousemove
  (function(){
    const reduce = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
    if(reduce) return;
    document.querySelectorAll('.tilt').forEach(el=>{
      const max = 10;
      el.addEventListener('mousemove', e=>{
        const r = el.getBoundingClientRect();
        const dx = (e.clientX - (r.left + r.width/2)) / (r.width/2);
        const dy = (e.clientY - (r.top + r.height/2)) / (r.height/2);
        el.style.transform = `perspective(900px) rotateX(${(-dy*max).toFixed(2)}deg) rotateY(${(dx*max).toFixed(2)}deg) translateZ(0)`;
      });
      el.addEventListener('mouseleave', ()=>{ el.style.transform = ''; });
    });
  })();

  // Auto-count flags
  (function(){
    const visited = document.querySelectorAll('#visited .flag-chip').length;
    const wish = document.querySelectorAll('#wishlist .flag-chip').length;
    const vc = document.getElementById('visited-count');
    const wc = document.getElementById('wish-count');
    if(vc) vc.textContent = `(${visited})`;
    if(wc) wc.textContent = `(${wish})`;
  })();
</script>
